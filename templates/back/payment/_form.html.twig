{% block ancv_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.amount) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.number) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}

{% block cash_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.amount) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}

{% block check_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.amount) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.number) }}</div>
    <div class="column">{{ form_row(form.cashingDate) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}

{% block discount_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.amount) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.discount) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}

{% block hello_asso_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.amount) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}

{% block refund_help_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.refundHelp) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.reference) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}

{% block transfer_payment_row %}
  <div class="columns">
    <div class="column">{{ form_row(form.amount) }}</div>
    <div class="column">{{ form_row(form.date) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.label) }}</div>
  </div>
  <div class="columns">
    <div class="column">{{ form_row(form.comment) }}</div>
  </div>
{% endblock %}
