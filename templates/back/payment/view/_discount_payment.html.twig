{% extends 'back/payment/view/_template.html.twig' %}

{% block detail %}
    <div class="columns">
        <div class="column">
            <span class="has-text-weight-bold">{{ 'back.payment.preview.discount'|trans }}</span>
            <span>{{ payment.discount }}</span>
        </div>
    </div>
{% endblock %}
